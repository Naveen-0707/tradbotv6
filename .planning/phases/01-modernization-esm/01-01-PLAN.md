# 01-01-PLAN: Syntax Migration & package.json Setup

**Wave**: 1
**Depends on**: Nothing
**Requirements**: MOD-01, MOD-02
**Autonomous**: true
**Files Modified**: bot.js, bridge.js, strategies.js, indicators.js, proto-decoder.js, backtest.js, setup.js, package.json

## Tasks

### 1. Initialize `package.json` [BLOCKING]
- <action>
  Create root `package.json` with correct ESM metadata and minimal dependencies.
- </action>
- <read_first>
  README.md (for project summary/metadata)
- </read_first>
- <acceptance_criteria>
  - `package.json` exists in root.
  - `package.json` contains `"type": "module"`.
  - `package.json` contains `"scripts": { "start": "node bridge.js" }`.
- </acceptance_criteria>

### 2. Modernize syntax in `strategies.js` and `indicators.js`
- <action>
  Convert CommonJS `require()` and `module.exports` to ESM `import`/`export` syntax.
- </action>
- <read_first>
  strategies.js, indicators.js
- </read_first>
- <acceptance_criteria>
  - `strategies.js` uses `export const ...` or `export { ... }`.
  - `indicators.js` uses `export const ...`.
  - No `require()` calls remain in these files.
- </acceptance_criteria>

### 3. Modernize syntax in `bot.js`, `bridge.js`, and `backtest.js` [BLOCKING]
- <action>
  1. Convert `require()` to `import`. Ensure file extensions (.js) are included in local imports.
  2. Implement `__dirname` and `__filename` shims using `import.meta.url`.
  3. Ensure `httpReq` and other internal helpers are correctly exported/imported.
- </action>
- <read_first>
  bot.js, bridge.js, backtest.js
- </read_first>
- <acceptance_criteria>
  - Files use `import` for built-in modules (e.g. `import fs from "fs"`).
  - Files use correct ESM-safe `__dirname` implementation.
  - Files initialize without syntax errors in Node.js --experimental-modules (default in v20+).
- </acceptance_criteria>

## Verification Criteria
### Goal: Modern ESM Execution
- [ ] `node --version` is >= 18.0.0.
- [ ] Running `node strategies.js` (empty run) or similar produces no syntax errors.
- [ ] `package.json` is correctly formatted.
- [ ] All `require()` calls are removed from target files.

## must_haves
- All imports between files must include the `.js` extension (MANDATORY).
- `__dirname` must be shimmed correctly in all entry points.
- No `module.exports` should remain in any file.
