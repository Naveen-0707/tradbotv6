# 01-02-PLAN: Environment & Test Modernization

**Wave**: 2
**Depends on**: 01-01
**Requirements**: MOD-03
**Autonomous**: true
**Files Modified**: setup.js, tests/indicators.smoke.js, tests/strategies.smoke.js, tests/bridge.api.smoke.js

## Tasks

### 1. Modernize ESM syntax in `setup.js`
- <action>
  Convert syntax and add `__dirname` shim.
- </action>
- <read_first>
  setup.js
- </read_first>
- <acceptance_criteria>
  - `setup.js` uses `import`/`export`.
  - `setup.js` uses ESM-safe `__dirname`.
- </acceptance_criteria>

### 2. Update and Verify Tests
- <action>
  1. Convert syntax to ESM in all smoke test files.
  2. Implement `import` for `assert` and `strategies.js`/`indicators.js`.
  3. Run each smoke test file directly via Node.js.
- </action>
- <read_first>
  tests/indicators.smoke.js, tests/strategies.smoke.js, tests/bridge.api.smoke.js
- </read_first>
- <acceptance_criteria>
  - All test files are in ESM format.
  - `node tests/indicators.smoke.js` passes.
  - `node tests/strategies.smoke.js` passes.
  - `node tests/bridge.api.smoke.js` passes.
- </acceptance_criteria>

## Verification Criteria
### Goal: Full Project ESM Compatibility
- [ ] All 3 smoke tests return a success message.
- [ ] No `require()` or `module.exports` calls remain in the `tests/` directory.

## must_haves
- All smoke tests must run successfully in the new ESM environment.
- Any Mock logic in tests should be correctly exported/imported with extensions.
