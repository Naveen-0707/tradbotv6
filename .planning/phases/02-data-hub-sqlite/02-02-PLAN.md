# Step Plan: 02-02: Data migration script (JSON to SQLite)

## Task
1. Write a function in `db.js` that checks if the `trades` table is empty but `fcb_trades.json` exists.
2. If so, read all objects from the JSON files and do bulk inserts into SQLite.
3. Rename the `.json` files to `.json.migrated.bak` so we don't migrate multiple times.

## Details
- Parse JSON.
- Add unique IDs if objects don't have them yet.
- Use Transactions for fast bulk insert.

## Verification
- Test initialization sequence to make sure old JSON data becomes SQL rows.
