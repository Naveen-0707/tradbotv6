# Step Plan: 02-03: Bot/Bridge logic integration for DB persistence

## Task
1. Modify `bot.js` to replace `fs.writeFileSync(TRD_FILE, ...)` with `dbSaveTrades(...)`.
2. Do the same for `fcb_signals.json`.
3. Modify `bridge.js` to poll `db.js` via `getAllTrades` instead of `fs.watchFile`.
4. Modify `backtest.js` to save directly to standard DB outputs, or keep writing to memory then use `saveTrades()`.
5. Update smoke tests (`bridge.api.smoke.js`) to test the new persistence API logic.

## Details
- Replace native Node fs watches with DB-backed polling.
- Make all API endpoint actions invoke DB queries directly. 

## Verification
- `npm run test:api` passes.
