# Step Plan: 03-01: UI Notification component and alert logic

## Task
1. Create a `<Toast />` component within `index.html` React app.
2. Hook up toast state (`const [toasts, setToasts] = useState([])`).
3. Have the SSE event listener in `index.html` listen for a `toast` message stream.
4. From `bridge.js` / `bot.js`, emit a toast event via SSE when Macro Guard triggers. Wait, `bot.js` runs in a separate process in live mode? It writes to logs, but it can also notify.

## Implementation details
- `bot.js` calls `notify()` which uses android intents, but it can also write to `fcb_cmd.json` or we can have a lightweight file `fcb_toasts.json` it appends to, which `bridge.js` watches. OR, since we already have SQLite, bot can insert into a `toasts` table? Too complex.
- Actually, `bridge.js` can parse `"🚨MACRO"` from the `bot.log` streaming and emit SSE! Or we just use `index.html` purely.

## Better simpler implementation
Bridge handles all UI. Bridge can watch `fcb_log_YYYY-MM-DD.txt` and when it sees `[WARN]` or `[ERROR]` or `[MACRO]` lines added, broadcast it via SSE as a Toast!
