# Step Plan: 03-02: Structured error handling in Bridge and UI integration

## Task
1. Modify `bridge.js` API endpoints so that failed promises or DB errors return a structured `{ ok: false, error: "..." }` instead of silent 500s or crashing the bridge.
2. In `index.html`, wrap `api.post` and `api.del` to catch those JSON errors and show a toast warning.
3. Add a "DB Connection" status badge next to the connection status logic in `index.html`. Bridge can emit `db_status: "online"` repeatedly inside its SSE loop, or check SQLite `db.open`.

## Implementation details
- `bridge.js` SSE connect loop: broadcast `{ type: "db_status", connected: true }`.
- `index.html` renders a green DB icon.
